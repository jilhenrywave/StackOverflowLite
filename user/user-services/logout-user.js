// const Token = require('../Token');
// // Get user id

// // delete token record or records

// //if nothing is deleted then trhow error

// const logoutUser = async (token, userId) => {
//   const deletedRecords = await Token.destroy({ where: { token, userId } });
//   console.log(deletedRecords);
// };

// module.exports = logoutUser;
